<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.kydmedfinal">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:icon="@drawable/heart_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".reset"></activity>
        <activity android:name=".login" />
        <activity android:name=".disease" />
        <activity android:name=".symptoms" />
        <activity android:name=".webactivity" />
        <activity android:name=".mymedtime" />
        <activity android:name=".Reaptingactivity" />
        <activity android:name=".coloramp" />
        <activity android:name=".weekdays.sundaytime" />
        <activity android:name=".weekdays.saturdaytime" />
        <activity android:name=".weekdays.fridaytime" />
        <activity android:name=".weekdays.thursdaytime" />
        <activity android:name=".weekdays.wednesdaytime" />
        <activity android:name=".weekdays.tuesdaytime" />
        <activity android:name=".mondaytime" />
        <activity android:name=".timewala" />
        <activity android:name=".takepass" />
        <activity android:name=".pass" />
        <activity android:name=".knowit" />
        <activity android:name=".splash" />
        <activity android:name=".MainActivity" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyCTtRPd9er9nlEqBkM2pAPqemayI8ox_VM" />

        <activity
            android:name=".MapActivity"
            android:label="@string/title_activity_map"
            android:theme="@style/AppTheme.NoActionBar" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <activity android:name=".brain" />
        <activity android:name=".kidney" />
        <activity android:name=".cancer" />
        <activity android:name=".liver" />
        <activity android:name=".aids" />
        <activity android:name=".amp" />
        <activity android:name=".permission">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver android:name=".RemainderBroadcast1" />
    </application>

</manifest>